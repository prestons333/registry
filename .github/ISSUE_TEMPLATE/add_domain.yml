name: Register a domain
description: Register a new subdomain
title: "[DOMAIN]: "
labels: ["register domain"]
assignees:
  - proudparrot2
body:
  - type: markdown
    attributes:
      value: |
        Please enter your chosen domain as the issue's title, after "[DOMAIN]".
  - type: dropdown
    id: domain
    attributes:
      label: Domain
      description: What domain do you want to use?
      options:
        - isreallycool.lol 
        - mathlearning.tech 
        - sorinsurance.org 
        - artclass.site
        - hotglue.click
        - englishpapers.online
        - ixl.plus
        - sciencesimplified.pro
        - msg.boats
    validations:
      required: true
  - type: dropdown
    id: record
    attributes:
      label: Record type
      description: What is the record's type?
      options:
        - A 
        - CNAME 
        - TXT 
    validations:
      required: true

  - type: input
    id: value
    attributes:
      label: Record value
      description: What's the record's value?
      placeholder: ex. google.com
    validations:
      required: true
      
  - type: input
    id: name
    attributes:
      label: Record name
      description: What's the record's name? This is what the subdomain will be.
      placeholder: ex. myfirstproject will be myfirstproject.domain.com
    validations:
      required: true

  - type: checkboxes
    id: proxied
    attributes:
      label: Cloudflare Proxy
      description: Do you want your DNS record to be proxied by Cloudflare?
      options:
        - label: Yes
          required: false

  - type: checkboxes
    id: agree
    attributes:
      label: Code of Conduct
      description: By submitting this record, you agree to our [Registry agreements](https://github.com/sparkdns/registry/blob/main/AGREEMENTS.md)
      options:
        - label: I agree to follow the Registry agreements
          required: true
          
  
